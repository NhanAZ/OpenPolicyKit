[project]
name = "example"
version = "0.1.0"
dependencies = [
    "requests>=2.28.0",
    "flask",
    "pytest==7.0.0"
]

[tool.poetry.dependencies]
python = "^3.9"
requests = "^2.28.0"
flask = "*"
numpy = "1.24.0"
